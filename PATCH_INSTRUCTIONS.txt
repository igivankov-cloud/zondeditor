PATCH INSTRUCTIONS (optional, later step)

Этот пакет добавляет структуру src/ + запуск run_zondeditor.py + модуль K4-парсера.
Сейчас запуск остаётся 100% совместимым: мы НЕ трогаем монолит.

Когда захочешь — вынесем K4-парсер из монолита в src/zondeditor/io/k4_reader.py.
Для этого:
1) В монолите добавь импорт:
   from src.zondeditor.io.k4_reader import detect_geo_kind, parse_k4_geo_strict

2) Там, где вызывается parse_k4_geo_strict(data) — замени на:
   parse_k4_geo_strict(data, TestData)

3) Удали/закомментируй локальные определения K4_SIG/detect_geo_kind/parse_k4_geo_strict/_k4_* из монолита,
   чтобы не было дублей.

Это сделаем отдельным коммитом после проверки запуска.
